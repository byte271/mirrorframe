{
  "name": "mirrorframe",
  "version": "0.4.0",
  "description": "Mirrorframe — capture a page, distill a Design Genome, recover its interaction state machines, reconstruct it in React, and verify per node + per state by visual diff.",
  "bin": { "mirrorframe": "scripts/mirrorframe.js" },
  "scripts": {
    "postinstall": "playwright install chromium",
    "demo": "node scripts/mirrorframe.js run --dir ./fixtures/northlight --out ./mf-out/northlight",
    "verify": "node scripts/verify-all.js"
  },
  "license": "MIT",
  "dependencies": {
    "esbuild": "^0.28.1",
    "pixelmatch": "^5.3.0",
    "playwright": "^1.61.1",
    "pngjs": "^7.0.0",
    "react": "^19.2.7",
    "react-dom": "^19.2.7"
  }
}
